# GB 18218-2018 字段映射说明（对应本系统数据库/计算逻辑）

说明：本项目提供“可配置参数表 + 可执行计算引擎”的实现框架，用于对齐 GB 18218-2018 的结构（式(1)、式(2)、表1/表2/表3/表4/表5/表6）。`db_init_gb18218.sql` 已按你提供的工程化描述写入表5（α）与表6（等级）的区间规则，并补充表1/表3的示例物质；其余标准表格数据请继续导入补全。

## 1) `chemicals`（危险化学品基础信息与临界量）

- `critical_quantity`：对应表1/表2（临界量 `Qi`）
- `hazard_category_symbol`：对应表3/表4 的类别符号（用于类别级兜底参数）
- `beta`：对应 β（表3优先；表4按类别兜底；本系统支持“物质级 β 优先”）
- `beta_source`：β 的来源说明（标准/文件/人工维护）
- `gb_version`：标准版本标识（默认 `GB 18218-2018`）

## 2) `storage_records`（企业-化学品储存数据）

- `amount`：对应 `qi`（某单元内某物质的实际储存量/存在量）
- `unit_name`/`unit_type`：对应“重大危险源单元”（装置/罐区/库区等）维度，用于分单元计算式(1)/式(2)
- `qty_basis`：数量口径（储存/生产/使用等），用于数据可追溯与审计说明
- `material_type`：`single`/`mixture`（单一物质/混合物）
- `mixture_name`/`mixture_category_symbol`：混合物名称与类别符号；当为混合物时，可用类别参数表兜底计算

## 3) `evaluation_results`（辨识与等级评估结果）

- `s_value`：对应式(1) 计算得到的 `S`
- `is_major_hazard`：对应“是否构成重大危险源”（一般以 `S ≥ 1` 判定）
- `exposure_people_500m`：对应表5 的“500m 范围暴露人口”
- `alpha_used`：对应表5 的 α（由 `exposure_people_500m` 查表得到）
- `r_value`：对应式(2) 计算得到的 `R`
- `level_by_gb`：对应表6 的等级判定结果（按 `R` 区间查表，区间：`R≥100` 一级，`50≤R<100` 二级，`10≤R<50` 三级，`R<10` 四级）
- `level`：兼容旧实现 `rule_sets.levels` 的等级（便于保留原系统规则）
- `gb_version`：本次评估采用的标准版本
- `basis`：保存计算明细（每种物质 `qi/Qi`、β、贡献值、单元汇总的 S/R、最终判定）

## 4) 参数表（建议/可选）

本系统引入参数表是为了避免把标准表格写死在代码里，便于后续更新与审计。

- `alpha_rules`：表5（暴露人口 → α）——建议必需（否则 α 默认 1.0）
- `level_rules`：表6（R → 等级）——建议必需（否则用 `rule_sets.levels` 兼容）
- `category_thresholds`：表2（类别符号 → 临界量 Qi 兜底，用于混合物/类别计算）——可选
- `category_betas`：表4（类别符号 → β 兜底，用于混合物/物质未配置 β）——可选

## 5) 公式实现位置

- GB 18218 计算逻辑：`app/services/identify.py`
- 评估入口（生成结果入库）：`app/views/user.py` 的 `evaluate()` 路由
